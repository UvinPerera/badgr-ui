version: "2"
services:
  node:
    image: "node:8"
    user: "node"
    working_dir: /home/node/app
    environment:
      - NODE_ENV=test
    volumes:
      - ./:/home/node/app
    expose:
      - "4200"
    command: "npm install && npm start"